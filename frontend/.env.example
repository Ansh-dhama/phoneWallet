# Recommended for local development with the included Vite proxy.
VITE_API_BASE_URL=
VITE_BACKEND_TARGET=http://localhost:8090

# If you deploy frontend and backend separately, set a real API URL instead:
# VITE_API_BASE_URL=https://api.example.com
